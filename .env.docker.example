# ─────────────────────────────────────────────────────
# Docker Compose Environment Variables
# ─────────────────────────────────────────────────────
# Copy this file to .env in the project root:
#   cp .env.docker.example .env
#
# Then replace the placeholder passwords with strong,
# unique passwords. Never commit the .env file!
# ─────────────────────────────────────────────────────

# MongoDB Root Admin (used only for initial setup & admin tasks)
MONGO_ROOT_USER=barber_root
MONGO_ROOT_PASSWORD=REPLACE_WITH_STRONG_PASSWORD_MIN_20_CHARS

# MongoDB Application User (used by backend — readWrite only)
MONGO_APP_PASSWORD=REPLACE_WITH_DIFFERENT_STRONG_PASSWORD

# SSL/TLS (only needed for production with docker-compose.prod.yml)
DOMAIN=app.tudominio.com
CERTBOT_EMAIL=admin@tudominio.com
